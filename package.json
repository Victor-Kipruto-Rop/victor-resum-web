{
  "name": "victor-resum-web",
  "version": "1.0.0",
  "private": true,
  "description": "Static portfolio/blog site deployed to Cloudflare Workers (assets-only) from ./public.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
